# shared/kernel

Utilidades verdaderamente transversales y sin estado (p. ej. generación de IDs, utilidades de fecha/hora en UTC) usadas por más de una capa. No contiene lógica de negocio ni reglas de dominio — si una utilidad encapsula una regla del negocio, pertenece a `domain`, no aquí.
